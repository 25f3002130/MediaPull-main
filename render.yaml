services:
  - type: web
    name: MediaPull
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 1 --threads 100 app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: SECRET_KEY
        generateValue: true
